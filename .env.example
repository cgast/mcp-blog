# Required: API token for authenticating LLM connections to the MCP server
MCP_API_TOKEN=change-me-to-a-secure-random-token

# Blog settings
BLOG_TITLE=My MCP Blog
BLOG_DESCRIPTION=A blog managed by AI via MCP
BLOG_BASE_URL=http://localhost:3000

# Storage — host path for blog post files (bind mount, persists across rebuilds)
POSTS_PATH=./data/posts

# Port (optional, default shown)
PORT=3000
